const nombre = "Alejo"
const apellido = "Perez"

    const persona = {
        nombre,
        apellido
    }   

// sessionStorage.setItem("usuario", JSON.stringify(persona))
// localStorage.setItem("usuario", JSON.stringify(persona))

// const expiration_date = new Date();
// expiration_date.setTime(expiration_date.getTime() + (2 * 60 * 1000)) 

// document.cookie = `nombrecookie=${JSON.stringify(persona)}; expires=` + expiration_date.toUTCString() + ";path=/"